Criar o package.json
npm init

//gerencia as requisições e rotas
npm install express

//reiniciar o servidor sempre q tem alterações
npm install -D nodemon

//instalar o mongo
npm install --save mongodb

//dependencia para q o js monitore o banco
npm install --save mongoose